# Plex local CLI configuration
# Copy to: .env

# Plex base URL (local on the NAS by default)
PLEX_BASE_URL=http://127.0.0.1:32400

# Required: Plex token (find it in Plex Web / XML, or from your existing setup)
PLEX_TOKEN=

# Docker container name
PLEX_CONTAINER=plex
